import React from 'react';
import { Header, Navbar } from './index.js';

const common = () => {
    return (
        <div>
            <Header />
           
        </div>
    );
};

export default common
